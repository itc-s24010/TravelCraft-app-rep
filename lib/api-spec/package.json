{
  "name": "@workspace/api-spec",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "codegen": "orval --config ./orval.config.ts && node -e \"const fs=require('fs');const f='../../lib/api-zod/src/generated/api.ts';let c=fs.readFileSync(f,'utf8');c=c.replace(/zod\\.int\\(\\)/g,'zod.number().int()');fs.writeFileSync(f,c);\" && pnpm -w run typecheck:libs"
  },
  "devDependencies": {
    "orval": "^8.23.0"
  }
}
